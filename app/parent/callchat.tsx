// Direct tab route for "Call & Chat" in the parent bottom navigation.
// Re-exports the full Call & Chat screen that also lives under (more) so the
// dashboard tile and the bottom-nav tab share one implementation.
export { default } from "./(more)/communicate";
