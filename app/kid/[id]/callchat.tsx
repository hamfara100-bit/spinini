// Top-level tab shim — lets "Call & Chat" appear in the bottom tab bar
// without the routing ambiguity of registering a (more)/* nested screen as a tab.
export { default } from "./(more)/communicate";
